# Client apps

The applications do have different target platforms and solve different user needs. Though many
UseCases are similar. Check if an implementation change in one app should be reflected by one or
more other apps.